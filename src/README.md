# src Directory

Folder ini berisi komponen pendukung API testing:

- `config/`: runtime config (base URL, token, timeout).
- `fixtures/`: data builder payload request.
- `utils/`: utility function umum.

Semua test utama ada di `tests/api/**`.
