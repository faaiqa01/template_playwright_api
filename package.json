{
  "name": "playwright-template",
  "version": "1.0.0",
  "description": "Playwright E2E Testing Template with Best Practices",
  "main": "index.js",
  "scripts": {
    "auth:setup": "playwright test tests/auth.setup.ts --project=setup",
    "test": "playwright test --project=chromium --no-deps",
    "test:e2e": "playwright test tests/e2e --project=chromium --no-deps",
    "test:integration": "playwright test tests/integration --project=chromium --no-deps",
    "test:unit": "playwright test tests/unit --project=chromium --no-deps",
    "test:ui": "playwright test tests/e2e --project=chromium --ui --no-deps",
    "test:headed": "playwright test --project=chromium --headed --no-deps",
    "test:debug": "playwright test --project=chromium --debug --no-deps",
    "test:report": "playwright show-report",
    "test:chrome": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "test:webkit": "playwright test --project=webkit",
    "test:mobile": "playwright test --project=\"Mobile Chrome\"",
    "template:cleanup": "node scripts/template-cleanup.mjs",
    "rtk": ".\\rtk-x86_64-pc-windows-msvc\\rtk.exe",
    "rtk:version": ".\\rtk-x86_64-pc-windows-msvc\\rtk.exe --version",
    "mcp:context7": "npx -y @upstash/context7-mcp@latest",
    "mcp:playwright": "npx @playwright/mcp@latest --isolated --storage-state=playwright/.auth/user.json",
    "install:browsers": "playwright install",
    "install:chrome": "playwright install chromium",
    "install:firefox": "playwright install firefox",
    "install:webkit": "playwright install webkit",
    "codegen": "playwright codegen",
    "lint": "tsc --noEmit",
    "rules:sync:to-root": "node scripts/sync-rules.mjs to-root",
    "rules:sync:from-root": "node scripts/sync-rules.mjs from-root"
  },
  "keywords": [
    "playwright",
    "e2e",
    "testing",
    "automation",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0"
  }
}
